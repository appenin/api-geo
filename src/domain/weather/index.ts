import { climaticRegionController, climaticRegionService } from './climatic_region';

export const weatherServices = {
  climaticRegionService,
};

export const weatherController = {
  climaticRegionController,
};
